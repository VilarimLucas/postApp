<div class="container text-center">
    <div class="row">
        <div class="col" style="padding-top: 1rem;">
            <div class="card border-secondary mb-3">
                <div class="card-header">Alterar de Postagens</div>
                <div class="card-body">

                    <h5 class="card-title"></h5>
                    <p class="card-text"></p>


                    <form action="/update" method="POST">
                        {{#each posts}}

                        <div class="container">
                            <div class="row">
                                <div class="col-9 text-align-left">
                                    <div class="mb-3">
                                        <p>Código: <input name="id" value="{{id}}"></p>
                                    </div>
                                </div>
                                <div class="col-sm-5 col-md-6">
                                    <div class="mb-3">
                                        <!--label class="picture" tabIndex="0" for="picture-input">
                                        ||||||enctype="multipart/form-data"||||||||||
                                            <input type="file" accept="image/*" name="imagem" id="picture-input"
                                                required>
                                            <span class="picture-image"></span>
                                            <script>
                                                const inputFile = document.querySelector('#picture-input');
                                                const pictureImage = document.querySelector('.picture-image');
                                                //const pictureImageTxt = 'Escolha uma Imagem';
                                                //pictureImage.innerHTML = pictureImageTxt;
                                                const img = document.createElement('img');
                                                img.classList.add('picture-img');
                                                img.src = "/images/{{imagem}}";
                                                pictureImage.appendChild(img);


                                                inputFile.addEventListener('change', function (e) {
                                                    const inputTarget = e.target;
                                                    const file = inputTarget.files[0];
                                                    if (file) {
                                                        const reader = new FileReader();

                                                        reader.addEventListener('load', function (e) {
                                                            
                                                            const readerTarget = e.target;
                                                            img.src = readerTarget.result;
                                                            pictureImage.innerHTML = '';
                                                            pictureImage.appendChild(img);
                                                        });

                                                        reader.readAsDataURL(file);
                                                    } else {
                                                        pictureImage.innerHTML = pictureImageTxt;
                                                    }

                                                });
                                            </script>
                                        </label -->
                                    </div>
                                </div>
                                <div class="col-sm-5 offset-sm-2 col-md-6 offset-md-0 text-align-left">
                                    <div class="mb-3">
                                        <p>Titulo:</p><input type="text" name="titulo" value="{{titulo}}"
                                            class="form-control form-control-lg">
                                    </div>
                                    <div class="mb-3">
                                        <p>Conteúdo:</p><textarea name="conteudo"
                                            class="form-control form-control-lg">{{conteudo}}</textarea>
                                    </div>
                                    <a href="/" class="btn btn-danger"><i class="bi bi-backspace"></i> cancelar</a>
                                    <button  type="submit" class="btn btn-primary"><i class="bi bi-vector-pen"></i> Alterar</button>
                                </div>
                            </div>
                        </div>

                        {{/each}}
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>